<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <!--스프링MVC 설정을 애너테이션 기반으로 처리하겠다는 의미, 스프링MVC의 여러객체들을 자동으로 스프링의 빈으로 등록-->
    <mvc:annotation-driven></mvc:annotation-driven>

    <!-- webapp 디렉토리 하위 resources 디렉토리(정적 파일들의 저장소) 의 위치를 등록
    /resources 경로로 들어오는 요청은 정적파일을 요구하는 것이므로  스프링MVC에서 처리하지 않는다는 의미-->
    <mvc:resources mapping="/resources/**" location="/resources/"></mvc:resources>

    <!--스프링MVC에서 제공하는 뷰(view)를 어떻게 결정할 것인지 설정을 담당하는 빈(객체)-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"></property> <!-- prefix : MVC에서 사용했던 WEB-INF/view 경로-->
        <property name="suffix" value=".jsp"></property>            <!-- suffix : '.jsp' 확장자 지정-->
    </bean>

    <!--@Controller 어노테이션 적용된 클래스들을 자동으로 Bean 등록 됨-->
    <context:component-scan base-package="com.multicampus.basic.controller"/>

    <!--Formatter를 servlet-context.xml에 적용하여 스프링의 빈으로 등록해야 합니다
    이 등록을 위해 FormattingConversionServiceFactioryBean객체를 스프링의 빈으로 등록해야 합니다
    등록후 FormattingConversionServiceFactorybean객체 안에 LocalDateFormatter을 추가해야 합니다
    -->
   <bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
        <property name="formatters">
            <set>
                <bean class="com.multicampus.basic.controller.formatter.LocalDateFormatter"/>
         <!--   <bean class="com.multicampus.basic.controller.formatter.CheckboxFormatter"/> -->
            </set>
        </property>

    </bean>
<!-- conversionServixe라는 빈을 등록한 후 스프링 MVC를 처리할 때 mvc : annotation-driven 설정을 반드시 해줘야 한다-->
  <mvc:annotation-driven  conversion-service="conversionService" />

</beans>