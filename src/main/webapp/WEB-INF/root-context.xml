<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mybatis="http://mybatis.org/schema/mybatis-spring"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd
       http://mybatis.org/schema/mybatis-spring
       http://mybatis.org/schema/mybatis-spring.xsd">


    <!-- MariaDB 연동, DataSource -->
    <bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
        <property name="driverClassName" value="org.mariadb.jdbc.Driver"></property>
        <property name="jdbcUrl" value="jdbc:mariadb://localhost:3306/webdb"></property>
        <property name="username" value="webuser"></property>
        <property name="password" value="webuser"></property>
        <property name="dataSourceProperties">
            <props>
                <prop key="cachePrepStmts">true</prop>
                <prop key="prepStmtCacheSize">250</prop>
                <prop key="prepStmtCacheSqlLimit">2048</prop>
            </props>
        </property>
    </bean>

    <!-- MyBatis, DataSource -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource"
          destroy-method="close">
        <constructor-arg ref="hikariConfig" />
    </bean>

    <!-- MyBatis에서 SQLSession을 만드는 SQLSessionFactory 객체 -->
    <!-- SQLSession을 통해서 Connection 생성, Sql 전달하고 결과를 리턴받는 구조 -->
    <bean id = "sqlSessionFactory" class = "org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"></property>
        <property name="mapperLocations" value="classpath:/mappers/**/*.xml"></property>
    </bean>

    <!-- 빈 개별 등록
    <bean class="com.multicampus.basic.sample.SampleDAO2"></bean>
    <bean class="com.multicampus.basic.sample.SampleService"></bean>-->

    <!-- 한꺼번에 빈 등록 -->
    <!--<context:component-scan base-package="com.multicampus.basic"/>-->

    <!-- Mybatis Mapper -->
    <mybatis:scan base-package="com.multicampus.basic.mapper"></mybatis:scan>

    <context:component-scan base-package="com.multicampus.basic.config"/>
    <context:component-scan base-package="com.multicampus.basic.service"/>





</beans>