<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.multicampus.basic.mapper.TimeMapper2">

    <select id="getNow" resultType="string">    /*id 속성 값은 메서드의 이름과 동일해야 한다!*/
        select now()
    </select>

</mapper>